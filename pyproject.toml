[project]
name = "etl"
version = "0.1.0"
description = "Pipeline ETL Qualidade Planta - Dash Qualidade"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["utils"]